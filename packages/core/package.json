{
  "name": "@frostleaf/core",
  "version": "0.1.0",
  "scripts": {
    "build": "dotnet fable src && esbuild --platform=node --bundle --minify --outfile=dist/bundle.js src/App.fs.js"
  },
  "devDependencies": {
    "esbuild": "0.14.42"
  }
}
